import React from 'react';

interface Alert {
    group: any,
    state: any,
    name: any,
    severity: any,
    description: any,
    summary: any,
    alerts: any
}

const Alerts = (alert: Alert[]): JSX.Element => {
    return (
        <>
        </>
    );
};

export default Alerts;
